import javafx.event.ActionEvent;
import javafx.event.EventHandler;
import javafx.scene.control.Button;

public class DriveButton extends Button {
	NewLevel level;
    public DriveButton(NewLevel level) {
        this.level = level;
    }

   
}
